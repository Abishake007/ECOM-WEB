# ===============================================================
# DATABASE CONFIGURATION
# ===============================================================
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ecom_proj_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=ecom_user
spring.datasource.password=StrongPassword123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================================
# JPA / HIBERNATE SETTINGS
# ===============================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging for easier debugging during testing
logging.level.org.hibernate=DEBUG
logging.level.com.zaxxer.hikari=DEBUG

# ===============================================================
# SECURITY & AUTHENTICATION (JWT)
# ===============================================================
ecom.app.jwtSecret=======================BezKoderSecretKey=========================
ecom.app.jwtExpirationMs=86400000

# ===============================================================
# STRIPE PAYMENT INTEGRATION (PHASE 1 & 2)
# ===============================================================
# Your specific Secret Key from the Stripe Dashboard
stripe.api.key=${STRIPE_SECRET_KEY}

# ===============================================================
# EMAIL NOTIFICATION SETTINGS (PHASE 4 - ASYNC)
# ===============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abishakearunagirinathan16@gmail.com
spring.mail.password=kkggwwgsjgmuwpvv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true